import { Account } from './account.entity';
import { Platform } from './platform.entity';
import { Player } from '../../player/entities/player.entity';

export const AuthEntities = [Account, Platform, Player];